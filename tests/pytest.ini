[pytest]
testpaths = tests
markers =
    smoke: Quick sanity checks
    slow: Tests that take >10s (export, full table load)
    destructive: Tests that modify data (edit, approve, flag toggle)
    visual: Tests for CSS/visual indicators
timeout = 60
